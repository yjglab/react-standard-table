export const iconStyle = {
  color: "text-slate-700",
  size: 16,
  strokeWidth: 2,
};

export const fontStyle = {};

export const tableColumnWidth = Array(100)
  .fill(null)
  .map((_, i) => i + 1);
